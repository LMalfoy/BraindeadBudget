# Implementation Checklist v2.0.2

- reshape the quick-add widget into a clearer compact CTA
- change the widget wording to `Add Expense`
- pick the most suitable widget family and layout for a simple action button
- make `Save Expense` clearly disabled or otherwise obviously unavailable when inputs are incomplete
- unify the primary green action styling across dashboard and add-expense flows
- make `Recent Expenses` rows on the dashboard tappable
- open history from a recent-expense tap without breaking current dashboard behavior
- verify the widget deep link, add-expense flow, and history flow still work end to end
