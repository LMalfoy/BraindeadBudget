# Implementation Checklist v2.0.3

- add `Daily Safe Spend` to the dashboard summary card
- compute the value against the active budget period, not only the calendar month
- keep `Remaining` as the main emphasized budget number
- remove `Available This Month` from the card
- add a small dashboard info button
- present a short explanation for `Remaining` and `Daily Safe Spend`
- verify the dashboard card still feels visually balanced after removing one row
- verify no regressions in onboarding, dashboard rendering, or expense entry
