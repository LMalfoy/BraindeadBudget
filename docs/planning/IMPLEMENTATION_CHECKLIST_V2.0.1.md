# Implementation Checklist v2.0.1

- add a `Quick Add Expense` widget using WidgetKit
- make the widget open the app directly into the add-expense flow
- add a direct submit action for numeric amount entry
- keep the new keyboard flow fast and thumb-friendly
- review terminology for consistency across budget and recurring statistics labels
- verify the existing add-expense and dashboard flows still behave correctly
- add or update focused tests where practical
